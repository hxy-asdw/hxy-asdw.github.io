# 2026-09-10

## 今天完成了什么

- 完成 `doupo-analysis` Case Study 的第二轮阅读体验整理，保持已有事实、截图和 `SOURCES.md` 不变。
- 将 `runtime-lab.png` 从正文栏内的小图调整为 02 / Getting the game apart 的主要视觉证据，保持原始比例、响应式缩放和 lazy loading。
- 重排 10000 局模拟结果：以 `10,000 simulated runs` 为最大主数字，下面分层展示达到 100 级、成帝数量、三条路径和运行时间。
- 加入 fixed aptitude 10 / fixed initial state 的条件说明，并保留 `xiandi: 0 observed` 不等于零概率的边界。
- 将 Exploration Flow 改成 01–07 的纵向编号路径：Web game → Locate runtime → Read source → Reconstruct rules → Build runtime lab → Batch simulation → Replay randomness。
- 压缩 03、04、06、07 的重复正文，保留动机、规则发现、问题扩大和可复现性的叙事。
- 强化 Seed 93 区域，突出 93、3395 simulated years、6 → 3401、20291 个 53-bit random values，并保留 `3401 = 6 + 3395`。

## 做出的决定

- Case Study 继续采用 editorial、quiet、technical、personal 的叙事，不改成 Problem / Solution / Result 或 README 结构。
- 只调整 Case Study 页面和局部样式；不改 Hero、首页其他 section、导航、全站设计系统或研究事实。
- 不重新运行模拟，不重新生成截图，不把 Markdown 报告做成伪造的视觉结果。

## 验证

- 在 375px、390px、430px、1440px CSS 视口检查，无横向 overflow。
- 两张截图均正常加载并保持原始比例。
- `Back to Lab` 链接存在，浏览器 Console 新增错误为 0。

## 当前状态

Case Study 页面内容和视觉层级已完成这一轮整理，网站仓库工作区已产生 Case Study 页面、样式和截图文件的提交内容，尚未在本日志写入前完成本轮日志提交。

## 下一步

- 观察页面实际发布后的长页阅读节奏，再决定是否需要单独的内容校对；不继续扩展 doupo 研究功能。
